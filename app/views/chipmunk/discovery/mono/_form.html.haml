#{link_to 'Here', chipmunk_description_path} one can find specific information regarding ChIPMunk parameters.
Toy examples for the #{link_to 'simple', chipmunk_discovery_mono_path(example: :simple) }, #{link_to 'weighted', chipmunk_discovery_mono_path(example: :weighted) }, #{link_to 'peak', chipmunk_discovery_mono_path(example: :peak) } data sets.
= task_form_for task, :as => :task, url: chipmunk_discovery_mono_path, html: {class: 'task_params chipmunk_discovery'} do |f|
  .motif_discovery_options
    = f.input :sequence_weighting_mode, as: :sequence_weighting_mode_selector
  .sequence_list
    = f.fields_for :sequence_list, task.sequence_list do |sequence_list_form|
      %h5 Paste a list of sequences in a textarea or select a file with sequences to scan
      .sequence_list
        .sequence_list_text= sequence_list_form.input :text, i18n_label: 'label.Chipmunk.MotifDiscoveryForm.sequence_list', :as => :text, i18n_description: 'task_parameters.Chipmunk.MotifDiscoveryForm.sequence_list'
      .sequence_list_file= sequence_list_form.input :file, i18n_label: 'label.Chipmunk.MotifDiscoveryForm.sequence_list_file', :as => :file, i18n_description: 'task_parameters.Chipmunk.MotifDiscoveryForm.sequence_list'

  Advanced options:
  .expand_button
    %a{href: '#'} [+]
  .advanced_options
    = f.input :speed_mode, as: :speed_mode_selector
    = f.input :min_motif_length, as: :integer, input_html: {min: 6, max: 30}
    = f.input :max_motif_length, as: :integer, input_html: {min: 6, max: 30}
    = f.input :occurences_per_sequence, as: :occurences_per_sequence_selector
    = f.input :motif_shape_prior, as: :motif_shape_prior_selector
    = f.input :gc_content, as: :gc_content_selector
  %br
  = f.submit 'Run', data: {disable_with: 'Sending request'}, class: 'btn btn-primary'
  = f.button :button, 'Reset', :type => 'reset', :class => 'btn btn-warning'
